echo "Checking lti-sync-drain script sync..."
scripts/check-drain-script-sync.sh

echo "Running yarn lint..."
yarn lint

echo "Running yarn build..."
yarn build

echo "Running lint-staged..."
npx lint-staged
