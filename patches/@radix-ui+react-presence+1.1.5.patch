diff --git a/node_modules/@radix-ui/react-presence/dist/index.js b/node_modules/@radix-ui/react-presence/dist/index.js
index 944abc2..b6a8907 100644
--- a/node_modules/@radix-ui/react-presence/dist/index.js
+++ b/node_modules/@radix-ui/react-presence/dist/index.js
@@ -155,17 +155,11 @@ function getAnimationName(styles) {
   return styles?.animationName || "none";
 }
 function getElementRef(element) {
-  let getter = Object.getOwnPropertyDescriptor(element.props, "ref")?.get;
-  let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
-  if (mayWarn) {
-    return element.ref;
-  }
-  getter = Object.getOwnPropertyDescriptor(element, "ref")?.get;
-  mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
-  if (mayWarn) {
+  if (Object.prototype.hasOwnProperty.call(element.props, "ref")) {
     return element.props.ref;
   }
-  return element.props.ref || element.ref;
+  const elementRefDescriptor = Object.getOwnPropertyDescriptor(element, "ref");
+  return elementRefDescriptor && "value" in elementRefDescriptor ? elementRefDescriptor.value : void 0;
 }
 var Root = Presence;
 //# sourceMappingURL=index.js.map
diff --git a/node_modules/@radix-ui/react-presence/dist/index.mjs b/node_modules/@radix-ui/react-presence/dist/index.mjs
index 0efe02a..737d094 100644
--- a/node_modules/@radix-ui/react-presence/dist/index.mjs
+++ b/node_modules/@radix-ui/react-presence/dist/index.mjs
@@ -119,17 +119,11 @@ function getAnimationName(styles) {
   return styles?.animationName || "none";
 }
 function getElementRef(element) {
-  let getter = Object.getOwnPropertyDescriptor(element.props, "ref")?.get;
-  let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
-  if (mayWarn) {
-    return element.ref;
-  }
-  getter = Object.getOwnPropertyDescriptor(element, "ref")?.get;
-  mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
-  if (mayWarn) {
+  if (Object.prototype.hasOwnProperty.call(element.props, "ref")) {
     return element.props.ref;
   }
-  return element.props.ref || element.ref;
+  const elementRefDescriptor = Object.getOwnPropertyDescriptor(element, "ref");
+  return elementRefDescriptor && "value" in elementRefDescriptor ? elementRefDescriptor.value : void 0;
 }
 var Root = Presence;
 export {
