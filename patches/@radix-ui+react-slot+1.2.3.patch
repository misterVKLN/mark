diff --git a/node_modules/@radix-ui/react-slot/dist/index.js b/node_modules/@radix-ui/react-slot/dist/index.js
index 39b5893..bae7ab3 100644
--- a/node_modules/@radix-ui/react-slot/dist/index.js
+++ b/node_modules/@radix-ui/react-slot/dist/index.js
@@ -123,16 +123,10 @@ function mergeProps(slotProps, childProps) {
   return { ...slotProps, ...overrideProps };
 }
 function getElementRef(element) {
-  let getter = Object.getOwnPropertyDescriptor(element.props, "ref")?.get;
-  let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
-  if (mayWarn) {
-    return element.ref;
-  }
-  getter = Object.getOwnPropertyDescriptor(element, "ref")?.get;
-  mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
-  if (mayWarn) {
+  if (Object.prototype.hasOwnProperty.call(element.props, "ref")) {
     return element.props.ref;
   }
-  return element.props.ref || element.ref;
+  const elementRefDescriptor = Object.getOwnPropertyDescriptor(element, "ref");
+  return elementRefDescriptor && "value" in elementRefDescriptor ? elementRefDescriptor.value : void 0;
 }
 //# sourceMappingURL=index.js.map
diff --git a/node_modules/@radix-ui/react-slot/dist/index.mjs b/node_modules/@radix-ui/react-slot/dist/index.mjs
index 510e77f..3bdb11f 100644
--- a/node_modules/@radix-ui/react-slot/dist/index.mjs
+++ b/node_modules/@radix-ui/react-slot/dist/index.mjs
@@ -83,17 +83,11 @@ function mergeProps(slotProps, childProps) {
   return { ...slotProps, ...overrideProps };
 }
 function getElementRef(element) {
-  let getter = Object.getOwnPropertyDescriptor(element.props, "ref")?.get;
-  let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
-  if (mayWarn) {
-    return element.ref;
-  }
-  getter = Object.getOwnPropertyDescriptor(element, "ref")?.get;
-  mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
-  if (mayWarn) {
+  if (Object.prototype.hasOwnProperty.call(element.props, "ref")) {
     return element.props.ref;
   }
-  return element.props.ref || element.ref;
+  const elementRefDescriptor = Object.getOwnPropertyDescriptor(element, "ref");
+  return elementRefDescriptor && "value" in elementRefDescriptor ? elementRefDescriptor.value : void 0;
 }
 export {
   Slot as Root,
